import styled from 'styled-components';

export const SvgPopUp = styled.div`
  background: rgba(0, 0, 0, .5);
  color: white;
  padding: 4px 8px;
  width: fit-content;
  border-radius: 2px;
  display: none;
  position: absolute;
  font-size: 12px;
`;
